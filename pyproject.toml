[project]
name = "agent-myla"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Raktim", email = "reachraktim@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
agent-myla = "agent_myla:main"

[build-system]
requires = ["uv_build>=0.11.13,<0.12.0"]
build-backend = "uv_build"
